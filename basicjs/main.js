/**
 * DATA TYPES, VARIABLES, STATEMENTS (LOOP, SELECTION)
 *
 * DATA TYPES
 *  primitive : string, numbers, boolean, null, undefined
 *    ES6 (ES2015) : symbol
 *    ES20 : BigInt
 *  object
 *
 * VARIABLES
 *  let, const
 */
const name = 'Reymon';
const age = 20;

// concatenation = penggabungan string
console.log('My name is ' + name + " I'm " + age + ' years old.');

// Template Literals
console.log(`My name is ${name}, I'm ${age} years old.`);
